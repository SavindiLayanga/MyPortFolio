// const assignmentCards = document.querySelectorAll('.assignment-card');
//
// assignmentCards.forEach(card => {
//     card.addEventListener('mouseenter', () => {
//         card.querySelector('.font-side').style.transform = 'rotateY(180deg)';
//         card.querySelector('.back-side').style.transform = 'rotateY(0deg)';
//     });
//
//     card.addEventListener('mouseleave', () => {
//         card.querySelector('.font-side').style.transform = 'rotateY(0deg)';
//         card.querySelector('.back-side').style.transform = 'rotateY(180deg)';
//     });
// });

